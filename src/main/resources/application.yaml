  spring:
    datasource:
      driverClassName: "org.postgresql.Driver"
      url: "jdbc:postgresql://localhost:6432/sherit"
      username: "postgres"
      password: "12345"

    jpa:
      hibernate.ddl-auto: none
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          jdbc.time_zone: UTC
          format_sql: true

    sql.init.mode: always

  logging:
    level:
      org:
        springframework:
          orm.jpa: INFO
          transaction: INFO
          transaction.interceptor: TRACE
          orm.jpa.JpaTransactionManager: DEBUG
        zalando.logbook: TRACE